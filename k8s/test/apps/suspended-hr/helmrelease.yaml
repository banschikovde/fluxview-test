apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: suspended-hr
  namespace: suspended-hr
spec:
  interval: 5m
  suspend: true
  chart:
    spec:
      chart: ./k8s/test/apps/backend/chart
      version: "0.1.0"
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
