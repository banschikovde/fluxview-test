apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: podinfo

resources:
  - configmap-values.yaml
  - helmrelease.yaml
  - ocirepository.yaml
  - namespace.yaml
  - secret-values.yaml

patches:
  - path: patches/podinfo.yaml
