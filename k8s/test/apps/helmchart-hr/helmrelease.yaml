apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: helmchart-hr
  namespace: helmchart-hr
spec:
  interval: 5m
  chartRef:
    kind: HelmChart
    name: shared-chart
    namespace: flux-system
  values:
    replicaCount: 1
